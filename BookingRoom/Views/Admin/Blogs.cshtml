@model IEnumerable<BookingRoom.Models.Blog>

@{
    ViewBag.Title = "Bài Viết";
    Layout = "~/Views/Shared/Admin/_AdminLayout.cshtml";
    var i = 1;
}

<div class="row">
    <div class="col-md-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title mb-3">
                    <a href="@Url.Action("CreateBlog", "Admin")" class="btn btn-success rounded-0 btn-sm">Tạo Bài Viết Mới</a>
                </h4>
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr class="text-center bg-dark text-light">
                            <th> # </th>
                            <th> Tiêu Đề </th>
                            <th> Chuyên Mục </th>
                            <th> Hành Động </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="text-center"> @i </td>
                                <td>@item.Title</td>
                                <td>@item.BlogCategory.Name</td>
                                <td class="text-center">
                                    <a href="@Url.Action("EditBlog", "Admin", new { BlogID = item.BlogID })" class="btn btn-sm btn-info">Chỉnh Sửa</a>
                                    |
                                    <a href="@Url.Action("DeleteBlog", "Admin", new { BlogID = item.BlogID })" class="btn btn-sm btn-danger">Xóa</a>
                                </td>

                            </tr>
                            i++;
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
